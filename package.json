{
    "name": "html-project",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "vite --host 0.0.0.0 --port 80",
        "start":"parcel --host 0.0.0.0 --port 80 index.html",
        "build": "vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "parcel": "^2.8.2",
        "vite": "^4.0.0"
    }
}
